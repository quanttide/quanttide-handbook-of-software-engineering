# 量潮软件工程工作手册

软件工程领域的手册，按三个层级组织：软件类别定义、框架选型、语言知识。

## 层级

### categories/ — 软件类别定义

每个子目录定义一类软件的统一约定，回答「这一类软件长什么样」。

第一类是 `contract`——平台契约：规定跨项目统一的技术栈与部署约定，涵盖前端 Studio、后端 Provider、CLI 管理工具三件套的语言分工（Go / Flutter / Rust）、桶命名、域名、IaC 目录（`manifests/terraform`）、质量门禁与可观测安全。

### frameworks/ — 框架手册

每个框架一本默认选型手册，回答「框架默认怎么选」。

第一本是 `flutter`——状态管理默认 Bloc，路由使用 Go router，界面默认 Material Design。

### languages/ — 语言手册

每种语言一本知识手册，回答「语言怎么处理」。

第一本是 `rust`——tree-sitter 解析器实现与 LanguageParser trait 设计。

## 三者关系

类别定义落到具体项目时，先查所属软件类别的定义，再按需查框架与语言手册；框架和语言手册为类别定义提供细节支撑。手册从实际项目实践提炼，随项目生长逐步补充。
